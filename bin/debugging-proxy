#!/usr/bin/env node
const path = require('path')

const port = process.env.PORT || 1337;

require(path.join(__dirname, '../index')).start(port).then(() => {
    console.log(`HTTP proxy listening on http://localhost:${port}`)
})
